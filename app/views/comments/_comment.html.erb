<%# Display the comment with " says:" appended to user's email; provide delete button %>
<li class="collection-item">
  <div class="comment-header">
    <h6><strong><%= comment.user == current_user ? "You" : comment.user.username %></strong> said:</h6>
    <% if current_user == comment.user %>
      <%= link_to 'DELETE', [comment.topic, comment], method: :delete, data: { confirm: "Are you sure?" }, class: "comment-delete" %>
    <% end %>
  </div>
  <div class="show-whitespace">
    <p><%= comment.body %></p>
  </div>
  <div class="right-align">
    Posted on <%= comment.created_at.strftime("%m/%d/%Y at %H:%M %Z") %>
  </div>
</li>
